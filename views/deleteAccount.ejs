<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete Account</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-zinc-900 text-white min-h-screen flex items-center justify-center">
    <div class="bg-zinc-800 shadow-lg rounded-2xl p-8 w-full max-w-md">
        <h1 class="text-2xl font-bold text-red-500 mb-4 text-center">Delete Account Permanently</h1>
        <p class="text-gray-300 mb-6 text-center">
            Deleting your account is <span class="text-red-400 font-semibold">permanent</span>
            and cannot be undone. All your data, posts, and settings will be erased forever.
        </p>

        <!-- Confirmation Form -->
        <form action="/delete-account/<%= user.username%>" method="POST" class="space-y-4">
            <label class="flex items-center space-x-2">
                <input type="checkbox" name="confirmCheck" required class="w-4 h-4 text-red-600 rounded">
                <span class="text-sm text-gray-300">
                    I understand that this action cannot be undone.
                </span>
            </label>

            <div>
                <label for="confirmText" class="block text-sm text-gray-400 mb-1">
                    Type <span class="text-red-400 font-bold">DELETE</span> to confirm:
                </label>
                <input type="text" id="confirmText" name="confirmText" required pattern="DELETE"
                    class="w-full p-2 rounded-lg bg-zinc-700 text-white focus:ring-2 focus:ring-red-500 outline-none">
            </div>

            <div class="flex justify-between mt-6">
                <a href="/profile" class="px-4 py-2 rounded-lg bg-gray-600 hover:bg-gray-500 transition">
                    Cancel
                </a>
                <button type="submit" class="px-4 py-2 rounded-lg bg-red-600 hover:bg-red-500 transition">
                    Delete Permanently
                </button>
            </div>
        </form>
    </div>
</body>

</html>